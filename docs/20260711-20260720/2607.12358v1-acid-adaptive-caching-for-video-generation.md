---
title: "ACID: Adaptive Caching for vIDeo generation"
title_zh: ACID：面向视频生成的自适应缓存
authors: "Om Agrawal, Saurabh Agarwal, Aditya Akella"
date: 2026-07-14
pdf: "https://arxiv.org/pdf/2607.12358v1"
tags: ["query:diff-accel"]
score: 9.0
evidence: 无训练缓存的视频扩散加速
tldr: "视频扩散模型推理缓慢，现有缓存加速方法使用固定阈值控制质量与速度的权衡，但导致非必要的质量损失。ACID通过监测漂移信号变化率，在关键步骤使用低阈值、在其他步骤积极缓存，实现自适应阈值切换。在TeaCache、EasyCache、DiCache三种方法和HunyuanVideo、Wan 2.1、CogVideoX三个模型上，ACID扩展了Pareto前沿，相比保守固定阈值实现高达38%额外加速，且质量损失可忽略（PSNR<0.3 dB）。该方法无需重新训练，即插即用。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.12358v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1444, \"height\": 368, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.12358v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1439, \"height\": 479, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.12358v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1435, \"height\": 551, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.12358v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1438, \"height\": 548, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.12358v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1438, \"height\": 549, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.12358v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1454, \"height\": 1394, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.12358v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1450, \"height\": 924, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.12358v1/fig-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 1453, \"height\": 961, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.12358v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1507, \"height\": 1315, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.12358v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1217, \"height\": 382, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.12358v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1229, \"height\": 381, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.12358v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1229, \"height\": 418, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.12358v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1287, \"height\": 918, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.12358v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1268, \"height\": 814, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.12358v1/table-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1234, \"height\": 813, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.12358v1/table-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 1249, \"height\": 668, \"label\": \"Table\"}]"
motivation: 现有动态缓存方法使用固定阈值强制质量与速度的权衡，但关键步骤需要低阈值以保持质量，而其他步骤可积极缓存。
method: ACID作为轻量级无训练包装器，监测现有方法的漂移信号变化率，动态切换低高阈值，实现自适应缓存策略。
result: "在三个方法和三个模型上，ACID扩展Pareto前沿，在TeaCache+HunyuanVideo上获得2.16x加速，相比固定阈值额外38%加速，质量损失可忽略。"
conclusion: ACID是信号无关、模块化的加速方案，无需重新训练即可提升现有缓存方法的性能与效率。
---

## 摘要
视频扩散模型能够生成高质量内容，但由于其顺序去噪过程，推理速度仍然较慢。基于缓存的加速方法通过复用中间模型输出来解决这一问题：领先的动态方法（如 TeaCache、EasyCache 和 DiCache）会累积漂移信号，并在累积漂移保持在固定阈值 τ 以下时跳过昂贵的模型评估。该阈值控制着一个明显的权衡——提高阈值可以加快生成速度，但会牺牲视觉质量；而降低阈值则保持质量但牺牲速度。我们表明这种权衡并非本质性的；它只是在整个去噪过程中保持 τ 恒定的人为结果。我们识别出关键步骤的存在——即漂移信号快速变化的时间步——并证明在这些步骤上选择性地应用低阈值，同时在其他步骤上积极缓存，可以在显著更高的推理速度下恢复大部分保守缓存的质量。基于这一见解，我们提出了 ACID，一种轻量级的免训练封装器，它监控每种方法现有漂移信号的变化率，以动态地在低阈值和高阈值之间切换。ACID 与信号无关且模块化：它不需要重新训练，可以直接插入现有的动态缓存方法中，而无需修改其核心机制。在三种缓存方法（TeaCache、EasyCache、DiCache）和三个开源视频扩散模型（HunyuanVideo、Wan 2.1、CogVideoX）上的评估表明，ACID 持续扩展了视觉质量与推理速度之间的帕累托边界，超越了任何固定阈值所能达到的效果。特别地，在使用 TeaCache 和 HunyuanVideo 时，ACID 相对于无缓存基线实现了高达 2.16 倍的加速，并且相对于保守的固定阈值基线实现了高达 38% 的额外加速，同时质量下降可忽略不计（<0.3 dB PSNR，<0.01 SSIM，<0.01 LPIPS）。

## Abstract
Video diffusion models produce high-quality generations but remain slow at inference due to their sequential denoising procedure. Caching-based acceleration methods address this by reusing intermediate model outputs: leading dynamic approaches such as TeaCache, EasyCache, and DiCache accumulate a drift signal and skip expensive model evaluations when accumulated drift stays below a fixed threshold τ. This threshold controls an apparent tradeoff - raising it yields faster generation at the cost of visual quality, while lowering it preserves quality but sacrifices speed. We show this tradeoff is not fundamental; it is an artifact of holding τ constant throughout denoising. We identify the existence of critical steps - timesteps where the drift signal changes rapidly - and show that applying a low threshold selectively at these steps while caching aggressively elsewhere recovers most of the quality of conservative caching at substantially higher inference speeds. Building on this insight, we propose ACID, a lightweight, training-free wrapper that monitors the rate of change of each method's existing drift signal to dynamically switch between a low and a high threshold. ACID is signal-agnostic and modular: it requires no retraining and plugs directly into existing dynamic caching methods without modifying their core mechanisms. Evaluated across three caching methods (TeaCache, EasyCache, DiCache) and three open-source video diffusion models (HunyuanVideo, Wan 2.1, CogVideoX), ACID consistently expands the Pareto frontier of visual quality versus inference speed beyond what any fixed threshold achieves. In particular, on TeaCache and HunyuanVideo, ACID achieves up to 2.16x speedup over the no-caching baseline, and up to 38% additional speedup over the conservative fixed-threshold baseline with negligible (<0.3 dB PSNR, <0.01 SSIM, <0.01 LPIPS) quality degradation.

---

## 论文详细总结（自动生成）

# ACID：面向视频生成的自适应缓存

## 1. 论文的核心问题与整体含义（研究动机和背景）

视频扩散模型生成质量高，但推理速度极慢（单张A100上生成约5秒视频需3~23分钟），主要瓶颈在于顺序去噪过程的高计算代价。已有的训练无关加速方法中，**动态缓存方法**（如TeaCache、EasyCache、DiCache）通过累积“漂移信号”并与固定阈值τ比较来决定是否跳过模型前向传播，实现了可观加速。然而，固定阈值导致一个看似固有的**质量-速度权衡**：高阈值加速多但质量下降严重，低阈值质量保持但加速有限。作者通过实验发现，这一权衡**并非本质**，而是由于整个去噪过程中使用单一固定阈值造成的。他们识别出**关键步骤**（漂移信号快速变化的时间步），在这些步骤上谨慎缓存（低阈值）、其余步骤积极缓存（高阈值），可以大幅改善帕累托前沿。基于此洞察提出ACID——一种轻量级、免训练的模块化封装器，可即插即用到现有动态缓存方法中，无需修改其核心机制。

## 2. 论文提出的方法论

### 核心思想
利用**漂移信号的二阶导数（即变化率的瞬时变化）** 来检测关键步骤：当信号变化率超出稳定区间`[-δ, δ]`时，认为处于动态/关键阶段，使用**低阈值**（保守缓存，保留质量）；当变化率在稳定区间内时，使用**高阈值**（积极缓存，提高速度）。这直接突破了固定阈值方法无法在过程中调整缓存激进程度的缺陷。

### 关键技术细节
- **信号通用性**：ACID不依赖特定信号类型。它只要求底层缓存方法提供一个标量漂移信号（如TeaCache的ΔTEMNI、EasyCache的`k×Δinput/||vt-1||`、DiCache的浅层探针特征变化）。
- **阈值切换判据**：在当前步骤信号`st`与前一步信号`sprev`之差的绝对值`|st - sprev|`是否小于等于δ。若`≤δ`，则选择`τhigh`；否则选择`τlow`。δ是控制稳定判断的预设超参数。
- **算法流程**（Algorithm 1）：
  1. 初始化累积漂移Σ=0，前一步信号`sprev=None`。
  2. 对每个去噪时间步t（从T到0）：
     - 如果是预热阶段（通常前几步），运行完整模型并更新缓存状态。
     - 否则，计算当前信号`st`，叠加到Σ。
     - 若`sprev`非空且`|st - sprev| ≤ δ` → 设置当前阈值τ=τhigh（稳定）；否则τ=τlow（动态）。
     - 若Σ ≤ τ，则复用之前输出（由底层方法重构）；否则运行完整模型、重置Σ=0。
  3. 更新`sprev = st`，继续下一时间步。
- **无需重新训练**：所有阈值仅影响推理时的决策，模型权重不变。

## 3. 实验设计

### 数据集/场景
- **提示词（Prompts）**：来自**VBench**基准的33个提示词，覆盖全部16个VBench评测维度，按11组（每组3个）均匀选取。
- **模型**：三个开源视频扩散模型：
  - HunyuanVideo T2V 720P（高分辨率复杂模型）
  - Wan 2.1 T2V 1.3B（中等规模）
  - CogVideoX 1.5-5B（另一架构风格）
- **底层缓存方法**：TeaCache（三个模型均评估）、EasyCache（仅HunyuanVideo和Wan 2.1，因无官方CogVideoX实现）、DiCache（同EasyCache）。每个方法-模型组合全部测试。

### 基准对比
- **无缓存基线**（full model every step）。
- **固定阈值方法**：每个方法-模型组合测试多个固定阈值（从低到高），如TeaCache+HunyuanVideo中τ=0.1, 0.2, 0.3等。
- **自适应ACID**：多个`(τlow, τhigh)`对，配合不同δ取值，从众多配置中选出帕累托最优的点。

### 评估指标
- **推理效率**：推理延迟（秒）和加速比（无缓存延迟/方法延迟）。
- **视觉质量**：PSNR↑、SSIM↑、LPIPS↓，均相对于无缓存基线的输出计算。

## 4. 资源与算力
- **硬件**：NVIDIA A100 80GB PCIe GPU（未说明数量，推测单卡运行），使用PyTorch框架。
- **训练时长**：方法为**训练无关**（training-free），因此无需训练，仅需推理测试。
- **推理时间**：每个视频生成，无缓存基线耗时：HunyuanVideo约1359秒，Wan 2.1约212~224秒，CogVideoX约1019秒（单卡A100）。
- **总实验量**：每个方法-模型组合需要对至少7~8种阈值配置（包括固定和自适应）各生成33个视频，总计约7×33×7≈1600+个视频生成任务，但文中未给出具体总GPU小时数。

## 5. 实验数量与充分性
- **数量**：覆盖3种模型×3种缓存方法（部分交叉），每个组合测试7~12种阈值配置，共用33个多样prompt。最终提供了详细的性能表格（附录B共8张表）和帕累托图（附录A共3张图）。实验量较为充分。
- **充分性**：
  - **优点**：覆盖了不同架构（DiT-base, U-Net等）和不同缓存信号；使用了标准化基准提示词（VBench）；对每个配置使用固定种子，公平对比。
  - **不足**：缺少与其它自适应方法的直接对比（如AdaCache基于内容复杂度的自适应、或基于分数蒸馏的方法）；未测试在Image/Video之外的其他扩散模型（如Latent Diffusion）；未对不同prompt类型（如动态vs静态）做单独分析；δ和阈值对的选择可能依赖经验，未给出自动调参方法。

## 6. 论文的主要结论与发现
- 固定阈值强制质量-速度权衡是**非本质**的，因为去噪过程不同阶段对缓存激进程度的敏感度不同。
- **关键步骤**（漂移信号快速变化的时间步）真实存在，且其位置与二阶导数稳定点对齐，可作为自适应阈值切换的自然锚点。
- ACID在**所有评估的三种缓存方法×三种模型组合上一致扩展了帕累托前沿**，即能在几乎相同质量下获得更高加速，或在相同加速下获得更好质量。
- **定量示例**：TeaCache+HunyuanVideo，ACID达到2.16×加速（固定低阈值仅1.57×，固定高阈值3.20×但质量损失大），PSNR仅下降0.28 dB（相对于低阈值固定模式），SSIM下降0.006，LPIPS上升0.009——可忽略不计。相对保守固定阈值获得**38%额外加速**。
- EasyCache和DiCache也有类似趋势，证明ACID对信号类型不敏感，具有通用性。

## 7. 优点
- **轻量级、训练无关**：无需额外训练或微调，直接作为包装器使用，计算开销可忽略（仅计算信号差分和阈值比较）。
- **模块化与即插即用**：不修改底层缓存方法的内部逻辑，只需暴露一个标量信号即可适配。兼容TeaCache、EasyCache、DiCache等主流方法。
- **理论基础扎实**：通过分析漂移信号二阶导数的稳定化时间点，给出了阈值切换的直观解释，且该模式在多个模型上复现。
- **实验覆盖全面**：在三种不同架构模型上验证，信号类型覆盖输入层、变换率、浅层特征，证明通用性。
- **结果客观**：使用标准VBench提示集和多种质量指标（PSNR/SSIM/LPIPS），且提供帕累托前沿图，清晰展示ACID优于任何固定阈值。

## 8. 不足与局限
- **超参数依赖**：需要手动选择`(τlow, τhigh, δ)`三参量，虽文中给出了基于二阶导数稳定点的启发式指导（如稳定点位置），但缺乏自动调优策略，在不同模型/任务上可能需要网格搜索。
- **模型覆盖有限**：仅测试3种开源视频模型，未覆盖Flux、Stable Video Diffusion、OpenSora等，也未测试图像扩散模型。ACID的泛化性未得到充分证明。
- **与其它加速方法的对比**：只与固定阈值基线对比，未与同类的自适应方法（如AdaCache的基于内容复杂度、或FasterCache的分阶段策略）进行比较。也未与其他训练无关加速（如稀疏注意力、高效求解器）结合分析。
- **计算开销**：虽然该方法计算量小，但需要计算信号的二阶差分，对于某些底层方法（如DiCache需要运行浅层探针），部分信号本身已有额外开销，ACID在此基础上可能增加微小延迟（文中未量化）。
- **可能存在的偏差**：评估视频全部使用固定种子且基于VBench提示集，但未进行多样本统计检验或方差分析；质量的比较仅针对无缓存基线，未考虑用户感知评价（如FID、CLIP score等）。
- **应用场景限制**：目前仅在单卡A100 GPU上评测，未探索多卡分布式推理或低端设备（如消费级GPU、移动终端）的表现。对于实时性或低延迟需求强的场景，仍需进一步验证。

（完）
