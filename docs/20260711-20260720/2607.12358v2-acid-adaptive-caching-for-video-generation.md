---
title: "ACID: Adaptive Caching for vIDeo generation"
title_zh: ACID：面向视频生成的自适应缓存
authors: "Om Agrawal, Saurabh Agarwal, Aditya Akella"
date: 2026-07-14
pdf: "https://arxiv.org/pdf/2607.12358v2"
tags: ["query:diff-accel"]
score: 9.0
evidence: 视频扩散模型的自适应缓存推理加速
tldr: 视频扩散模型因顺序去噪过程推理缓慢，现有动态缓存方法使用固定阈值控制质量-速度权衡。本文发现存在关键步骤（漂移信号快速变化），在这些步骤应用低阈值、其余步骤积极缓存可恢复大部分质量。提出ACID，一种轻量级无训练包装器，通过监测漂移变化率动态切换阈值，可直接插入TeaCache等方法。实验在三个模型上实现最高2.16倍加速，质量损失可忽略（<0.3 dB PSNR）。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.12358v2/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1444, \"height\": 368, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.12358v2/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1439, \"height\": 479, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.12358v2/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1435, \"height\": 551, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.12358v2/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1438, \"height\": 548, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.12358v2/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1438, \"height\": 549, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.12358v2/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1454, \"height\": 1394, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.12358v2/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1450, \"height\": 924, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.12358v2/fig-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 1453, \"height\": 961, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.12358v2/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1507, \"height\": 1315, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.12358v2/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1217, \"height\": 382, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.12358v2/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1229, \"height\": 381, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.12358v2/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1229, \"height\": 418, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.12358v2/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1287, \"height\": 918, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.12358v2/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1268, \"height\": 814, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.12358v2/table-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1234, \"height\": 813, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.12358v2/table-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 1249, \"height\": 668, \"label\": \"Table\"}]"
motivation: 现有动态缓存方法使用固定阈值，导致生成速度与视觉质量之间存在伪权衡，实际可同时提升性能。
method: 提出ACID，通过监测漂移变化率动态切换低/高阈值，在关键步骤用低阈值保护质量，其他步骤用高阈值积极缓存。
result: "在TeaCache、EasyCache、DiCache和HunyuanVideo等模型上，ACID比固定阈值方法提速最高38%，质量损失可忽略。"
conclusion: ACID以轻量无训练方式扩展了质量-速度Pareto前沿，可即插即用提升现有动态缓存方法的效率。
---

## 摘要
视频扩散模型能够生成高质量的视频，但由于其顺序去噪过程，推理速度仍然较慢。基于缓存的加速方法通过复用中间模型输出来解决这一问题：主导的动态方法如TeaCache、EasyCache和DiCache会累积漂移信号，并在累积漂移低于固定阈值τ时跳过昂贵的模型评估。该阈值呈现出一个明显的权衡——提高阈值可加快生成速度，但牺牲视觉质量；降低阈值则保留质量但牺牲速度。我们表明这种权衡并非根本性的；它是整个去噪过程中保持τ恒定的人为产物。我们识别出关键步骤的存在——即漂移信号快速变化的时间步——并表明在这些步骤中选择性地应用低阈值，而在其他地方积极缓存，可以在显著更高的推理速度下恢复保守缓存的大部分质量。基于这一见解，我们提出了ACID，一种轻量级、无需训练的外挂模块，它监视每种方法现有漂移信号的变化率，以动态地在低阈值和高阈值之间切换。ACID是信号无关且模块化的：它无需重新训练，可直接插入现有的动态缓存方法中，而不修改其核心机制。在三种缓存方法（TeaCache、EasyCache、DiCache）和三种开源视频扩散模型（HunyuanVideo、Wan 2.1、CogVideoX）上的评估表明，ACID持续地将视觉质量与推理速度的帕累托前沿扩展到任何固定阈值所能达到的范围之外。特别是，在TeaCache和HunyuanVideo上，ACID相比无缓存基线实现了高达2.16倍的加速，相比保守的固定阈值基线实现了高达38%的额外加速，而质量下降可以忽略不计（<0.3 dB PSNR，<0.01 SSIM，<0.01 LPIPS）。

## Abstract
Video diffusion models produce high-quality generations but remain slow at inference due to their sequential denoising procedure. Caching-based acceleration methods address this by reusing intermediate model outputs: leading dynamic approaches such as TeaCache, EasyCache, and DiCache accumulate a drift signal and skip expensive model evaluations when accumulated drift stays below a fixed threshold $τ$. This threshold controls an apparent tradeoff - raising it yields faster generation at the cost of visual quality, while lowering it preserves quality but sacrifices speed. We show this tradeoff is not fundamental; it is an artifact of holding $τ$ constant throughout denoising. We identify the existence of critical steps - timesteps where the drift signal changes rapidly - and show that applying a low threshold selectively at these steps while caching aggressively elsewhere recovers most of the quality of conservative caching at substantially higher inference speeds. Building on this insight, we propose ACID, a lightweight, training-free wrapper that monitors the rate of change of each method's existing drift signal to dynamically switch between a low and a high threshold. ACID is signal-agnostic and modular: it requires no retraining and plugs directly into existing dynamic caching methods without modifying their core mechanisms. Evaluated across three caching methods (TeaCache, EasyCache, DiCache) and three open-source video diffusion models (HunyuanVideo, Wan 2.1, CogVideoX), ACID consistently expands the Pareto frontier of visual quality versus inference speed beyond what any fixed threshold achieves. In particular, on TeaCache and HunyuanVideo, ACID achieves up to 2.16x speedup over the no-caching baseline, and up to 38% additional speedup over the conservative fixed-threshold baseline with negligible (<0.3 dB PSNR, <0.01 SSIM, <0.01 LPIPS) quality degradation.

---

## 论文详细总结（自动生成）

# ACID: 面向视频生成的自适应缓存 —— 论文详细总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **背景**：视频扩散模型（如 HunyuanVideo、Wan 2.1、CogVideoX）因顺序去噪过程导致推理极慢（约 3~23 分钟生成 5 秒视频），成为部署瓶颈。现有加速方法分为训练型和训练无关型，其中基于缓存的训练无关方法（如 TeaCache、EasyCache、DiCache）通过复用前一步模型输出来减少计算，这些方法均采用**固定阈值 τ**：当累积的“漂移信号”低于阈值时跳过模型前向，否则执行完整模型。
- **核心问题**：固定阈值导致一个明显的速度-质量权衡：高阈值加速快但质量损失大，低阈值质量好但加速少。论文提出这一权衡并非根本性，而是因为整个去噪过程使用同一阈值——实际上存在“关键步骤”（漂移信号快速变化的 timestep），在这些步骤保守缓存，其他步骤积极缓存，可以同时获得高质量和高速度。
- **整体含义**：论文旨在打破固定阈值的人为限制，提出一种自适应阈值机制，在不改变原始缓存方法核心机制的前提下，扩展帕累托前沿，实现更优权衡。

## 2. 论文提出的方法论

- **核心思想**：基于对现有缓存信号（如 TeaCache 的 ∆TEMNI, EasyCache 的变换率 k, DiCache 的浅层特征变化）的分析，发现信号二阶导数的稳定点与最优双阈值切换点高度吻合。因此，通过监测信号的变化率（一阶差分），动态选择低阈值（τ_low）或高阈值（τ_high）。
- **关键技术细节**：
  - 定义稳定区域：当当前信号与前一信号的差值绝对值 ≤ δ（稳定边界）时，认为处于稳定阶段，使用高阈值 τ_high（更积极缓存）；否则处于动态阶段，使用低阈值 τ_low（保守缓存）。
  - 算法流程（Algorithm 1）：
    1. 初始化累积漂移 Σ=0，上一信号 s_prev=None。
    2. 对于每个去噪步 t（从 T 到 0）：
       - 若处于预热阶段（由基础方法 M 定义），执行完整模型并更新 M。
       - 否则，计算当前信号 s_t，加到 Σ。
       - 根据 |s_t - s_prev| ≤ δ 判断稳定/动态，选择 τ。
       - 若 Σ ≤ τ，跳过模型，利用 M 的重建方法复用输出；否则执行完整模型，重置 Σ=0。
       - 更新 s_prev = s_t。
  - ACID 是**信号无关和模块化**的：可直接包装在 TeaCache、EasyCache、DiCache 之上，无需修改其内部近似方法，也无需额外训练。
- **公式/算法**：无复杂公式，核心为二阶导数启发式的阈值切换。

## 3. 实验设计

- **模型与缓存方法**：
  - 模型：HunyuanVideo T2V 720P, Wan 2.1 T2V 1.3B, CogVideoX 1.5-5B。
  - 缓存方法：TeaCache（三个模型），EasyCache 和 DiCache（HunyuanVideo 和 Wan 2.1，因官方实现限制未在 CogVideoX 上评估）。
- **对比方法**：无缓存基线、各固定阈值配置（多个 τ 值）、ACID 自适应双阈值配置（多种 τ_low / τ_high 组合）。
- **评估指标**：
  - 推理效率：延迟（秒）与加速比（无缓存基线延迟 / 方法延迟）。
  - 视觉质量：与无缓存基线输出的 LPIPS、PSNR、SSIM。
- **数据集/提示**：从 VBench 提示集选取 33 个提示，涵盖 16 个 VBench 维度，每个维度至少 1 个提示（11 个组，每组 3 个）。固定随机种子。
- **实现细节**：NVIDIA A100 80GB PCIe GPU，PyTorch。

## 4. 资源与算力

- 文中未明确说明使用的 GPU 数量、训练时长（因无需训练）或具体的总计算量。提及实验使用 NVIDIA A100 80GB PCIe GPU，但未提多少张或总耗时。推测为单卡测试，因为推理评测通常单卡即可，且论文未强调多卡并行。

## 5. 实验数量与充分性

- **实验数量**：对于 7 个方法-模型组合（TeaCache × 3，EasyCache × 2，DiCache × 2），每个组合测试了多个固定阈值（3~8 个不等）和多个自适应配置（2~10 个不等），共约 60+ 个配置点。每个配置生成 33 个视频，总视频数约 2000+。
- **充分性**：实验覆盖了三种主流视频扩散模型、三种代表性动态缓存方法，提示来自标准基准 VBench，指标包括速度和质量。并且提供了帕累托前沿图和详细数据表。
- **客观公平**：所有配置使用相同种子，对比是基于无缓存基线的保真度，而非绝对视觉质量（避免了主观性），定量指标客观。但未与训练型加速方法或非缓存类训练无关方法（如蒸馏、ODE求解器）比较，可能局限。

## 6. 论文的主要结论与发现

- **关键发现**：扩散过程中存在“关键步骤”，在这些步骤缓存信号变化剧烈，固定阈值无法同时兼顾质量与速度；自适应阈值可以显著改善帕累托前沿。
- **ACID 效果**：
  - 在 TeaCache + HunyuanVideo 上，相比保守固定阈值（τ=0.1）额外加速 38%（2.16× vs 1.57×），PSNR 仅下降 0.28 dB（24.41 vs 24.69），SSIM 和 LPIPS 几乎不变。
  - 在 EasyCache 和 DiCache 上类似：自适应模式在接近低阈值质量的同时获得接近高阈值的加速。
  - 所有 7 个组合中，ACID 点均位于固定阈值帕累托纯前沿之外（即严格占优）。
- **通用性**：ACID 方法独立于具体缓存信号，可即插即用；且不同模型信号模式不同（U 形或其它），ACID 均有效。

## 7. 优点

- **轻量无训练**：无需额外训练或微调，计算开销极低（仅需记录前一信号并做一次比较）。
- **模块化与通用性**：可直接包装在现有动态缓存方法上，无需修改核心代码，适用多种模型和方法。
- **理论洞察**：揭示固定阈值的人为产物本质，提出“关键步骤”概念，并利用二阶导数启发式有效识别，具有理论价值。
- **显著改进**：在保持相近视觉质量下取得明显额外加速，且帕累托前沿扩展稳定。
- **实验全面**：跨模型、跨方法、多提示、多指标，结果可信。

## 8. 不足与局限

- **实验覆盖有限**：仅评测三种视频扩散模型，未覆盖图像扩散模型或更轻量模型（如 Stable Diffusion 系列）；也未与训练型加速方法（如蒸馏、量化）或非缓存类训练无关方法（如 Sparse Attention, DPM-Solver）比较，无法评估整体加速优势。
- **未讨论超参数敏感度**：双阈值 τ_low、τ_high 和稳定边界 δ 需要手动设定，论文未提供自动选取方法或鲁棒性分析，实际使用需调参。
- **信号假设依赖**：方法有效性依赖于二阶导数稳定点与最优切换点一致，虽然实验验证，但未理论证明，可能存在反例。
- **质量损失**：即使微小，自适应模式仍比低阈值模式有可察觉质量下降（PSNR 下降 <0.3 dB），在极度关注质量的应用中可能不可接受。
- **仅评测加速比 vs 质量**：未评测内存或能耗等其他资源指标。
- **CogVideoX 上仅评测 TeaCache**：EasyCache 和 DiCache 因官方不支持，未覆盖，可能降低通用性论证的强度。

（完）
